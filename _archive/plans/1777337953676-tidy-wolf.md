# 街機廳「讓 char 一起遊玩」功能實現計劃

## 目標
讓角色 (char) 陪伴使用者在街機廳一起遊玩，包含：
1. 角色在地圖上跟隨玩家
2. 雙人合作模式（輪流玩或一起控制）
3. 對話互動（角色根據遊戲情況說話）

---

## 一、地圖上角色跟隨玩家

### 1.1 新增 Character 類別
**檔案**: `apps/arcade/map/character.js`

```javascript
class Character {
  constructor(mapEngine, options = {}) {
    // 類似 Player 的結構
    this.x = options.x || 8;  // 初始位置在玩家附近
    this.y = options.y || 12;
    this.floor = options.floor || '1F';
    this.direction = 'down';
    this.isMoving = false;
    
    // 角色資訊（從 localStorage 讀取）
    this.name = options.name || 'AI 助理';
    this.avatar = options.avatar || '';
    this.personality = options.personality || '';
    
    // 跟隨邏輯
    this.followTarget = null;  // 跟隨目標（玩家）
    this.followDelay = 2;      // 跟隨延遲（格數）
    this.pathQueue = [];       // 玩家移動路徑佇列
  }
  
  // 跟隨玩家移動
  followPlayer(player) {
    // 記錄玩家路徑
    // 延遲後跟隨
  }
  
  // 渲染角色
  render(ctx, camera) {
    // 顯示頭像或角色外觀
    // 顯示名稱標籤
  }
}
```

### 1.2 修改 ArcadeGame 類別
**檔案**: `apps/arcade/arcade-game.js`

- 在 `setupPlayer()` 後新增 `setupCharacter()`
- 在 `update()` 中更新角色位置
- 在 `render()` 中渲染角色

### 1.3 讀取角色設定
從 localStorage 讀取：
- `sx_char_name` - 角色名稱
- `sx_char_avatar` - 角色頭像
- `sx_char_personality` - 角色性格

---

## 二、雙人合作模式

### 2.1 遊戲模式切換
在遊戲 Modal 中新增模式選擇：
- **單人模式** - 使用者自己玩
- **雙人模式** - 使用者和角色輪流

### 2.2 角色遊玩邏輯（根據性格決定表現）
**檔案**: `apps/arcade/character-player.js`

角色的遊戲表現會根據 `sx_char_personality` 自動調整：

```javascript
class CharacterPlayer {
  constructor(character, gameType) {
    this.character = character;
    this.gameType = gameType;
    this.skill = this.calculateSkillFromPersonality();
  }
  
  // 根據角色性格關鍵字計算技能等級
  calculateSkillFromPersonality() {
    const personality = this.character.personality.toLowerCase();
    
    // 分析性格關鍵字來決定遊戲風格
    if (personality.includes('認真') || personality.includes('完美主義') || personality.includes('聰明')) {
      return { accuracy: 0.85, reaction: 0.8, style: 'careful' };
    } else if (personality.includes('活潑') || personality.includes('衝動') || personality.includes('熱血')) {
      return { accuracy: 0.6, reaction: 0.9, style: 'aggressive' };
    } else if (personality.includes('冷靜') || personality.includes('沉著') || personality.includes('理智')) {
      return { accuracy: 0.75, reaction: 0.7, style: 'balanced' };
    } else if (personality.includes('笨拙') || personality.includes('迷糊') || personality.includes('天然')) {
      return { accuracy: 0.4, reaction: 0.5, style: 'clumsy' };
    } else if (personality.includes('傲嬌') || personality.includes('傲嬌')) {
      return { accuracy: 0.7, reaction: 0.75, style: 'tsundere' };
    }
    
    // 預設中等表現
    return { accuracy: 0.65, reaction: 0.65, style: 'balanced' };
  }
  
  // 角色自動玩遊戲
  play(gameState) {
    switch(this.gameType) {
      case 'snake': return this.playSnake(gameState);
      case 'tetris': return this.playTetris(gameState);
      // ...
    }
  }
  
  // 貪吃蛇 AI（根據 skill.accuracy 決定決策品質）
  playSnake(gameState) {
    // 根據 skill.accuracy 決定是否做出最佳決策
    // 根據 skill.style 決定遊玩風格
  }
}
```

### 2.3 輪流機制
```javascript
// 在遊戲中新增
let currentTurn = 'player';  // 'player' 或 'character'
let turnDuration = 30000;    // 每回合 30 秒

function switchTurn() {
  currentTurn = currentTurn === 'player' ? 'character' : 'player';
  if (currentTurn === 'character') {
    characterPlayer.play(gameState);
  }
}
```

---

## 三、對話互動系統

### 3.1 角色對話引擎
**檔案**: `apps/arcade/character-dialogue.js`

```javascript
const DIALOGUE_TEMPLATES = {
  // 遊戲開始
  gameStart: [
    "這遊戲看起來很有趣！",
    "一起加油吧！",
    "我準備好了！"
  ],
  // 玩家得分
  playerScore: [
    "太厲害了！",
    "哇！好棒！",
    "繼續保持！"
  ],
  // 玩家失誤
  playerMistake: [
    "沒關係，再試一次！",
    "加油！你可以的！",
    "別灰心～"
  ],
  // 角色自己玩
  characterPlay: [
    "換我試試看！",
    "讓我來！",
    "看我的！"
  ],
  // 角色得分
  characterScore: [
    "耶！我做到了！",
    "怎麼樣？不錯吧！",
    "嘿嘿～"
  ],
  // 角色失誤
  characterMistake: [
    "哎呀...失敗了",
    "嗚...好難",
    "下次會更好的！"
  ]
};

class CharacterDialogue {
  constructor(character) {
    this.character = character;
    this.lastDialogueTime = 0;
    this.cooldown = 3000;  // 對話冷卻時間
  }
  
  // 根據情境產生對話
  generateDialogue(situation, context = {}) {
    // 根據角色性格調整語氣
    // 隨機選擇對話
  }
  
  // 顯示對話泡泡
  showDialogue(text) {
    // 在角色頭上顯示對話泡泡
  }
}
```

### 3.2 對話觸發時機
- 遊戲開始/結束
- 玩家得分/失誤
- 角色得分/失誤
- 切換回合
- 遊戲中的特殊事件

### 3.3 角色性格影響
根據 `sx_char_personality` 調整對話風格：
- 活潑型：更多驚嘆、鼓勵
- 冷靜型：簡短、理性
- 傲嬌型：口是心非的鼓勵

---

## 四、UI 調整

### 4.1 地圖介面
- 角色顯示在玩家後方
- 角色名稱標籤
- 對話泡泡

### 4.2 遊戲介面
- 模式選擇按鈕（單人/雙人）
- 當前回合指示器
- 角色頭像和狀態
- 角色對話區域

### 4.3 CSS 新增
**檔案**: `apps/arcade/arcade.css`

```css
/* 角色樣式 */
.character-sprite { ... }
.character-name-tag { ... }
.dialogue-bubble { ... }

/* 遊戲模式選擇 */
.game-mode-selector { ... }

/* 回合指示器 */
.turn-indicator { ... }
```

---

## 五、實現步驟

### Phase 1: 角色跟隨 (基礎)
1. 建立 `apps/arcade/map/character.js`
2. 修改 `arcade-game.js` 整合角色
3. 新增 CSS 樣式
4. 測試角色在地圖上跟隨玩家

### Phase 2: 對話系統
1. 建立 `apps/arcade/character-dialogue.js`
2. 實作對話泡泡 UI
3. 連接遊戲事件觸發對話
4. 根據角色性格調整對話

### Phase 3: 雙人合作模式
1. 建立 `apps/arcade/character-player.js`
2. 修改遊戲 Modal 新增模式選擇
3. 實作輪流機制
4. 為各遊戲實作角色 AI

### Phase 4: 整合測試
1. 測試所有功能整合
2. 優化效能
3. 修復 bug

---

## 六、需要修改的檔案

| 檔案 | 修改內容 |
|------|----------|
| `apps/arcade/map/character.js` | **新增** - Character 類別 |
| `apps/arcade/character-dialogue.js` | **新增** - 對話系統 |
| `apps/arcade/character-player.js` | **新增** - 角色遊玩 AI |
| `apps/arcade/arcade-game.js` | 整合角色跟隨 |
| `apps/arcade/arcade.js` | 遊戲模式選擇、雙人邏輯 |
| `apps/arcade/arcade.html` | 引入新 JS 檔案 |
| `apps/arcade/arcade.css` | 新增角色相關樣式 |

---

## 七、技術考量

### 7.1 效能
- 角色移動不影響玩家操作
- 對話泡泡使用 CSS 動畫
- 避免過度渲染

### 7.2 兼容性
- 若無角色設定，不顯示角色功能
- 支援觸控和鍵盤操作

### 7.3 擴展性
- 對話模板可擴充
- 支援新增遊戲的角色 AI
